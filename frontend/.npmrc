# Anti-supply-chain-worm hardening (per MIGRATION.md):
# - minimum-release-age: only install package versions published >= 30 days ago
#   (43200 minutes), so a freshly-compromised release is never pulled.
# - save-exact: pin exact versions, never ranges.
minimum-release-age=43200
save-exact=true
